ktor { # this block will auto-reading by ktor lib
    deployment {
        port = 8080
    }
    application {
        modules = [com.github.nepyh.ebazi.BootstrapperKt.devModule]
    }
}
storage {
    dbUser = "admin"
    dbPassword = "password123"
}
